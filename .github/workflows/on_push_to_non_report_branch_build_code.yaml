name: On push on any non-report branch build

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'Set up Cloud SDK'
        uses: 'google-github-actions/setup-gcloud@v3'
        with:
          version: '>= 363.0.0'

      - name: 'Use gcloud CLI'
        run: 'gcloud info'
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: adopt
          java-version: 21
          cache: 'maven'
      - name: List active profiles per project
        run: mvn help:active-profiles
      - name: Build package
        run: |
          mvn --batch-mode install \
            -Dtech-lab-ingester.github.token=${GITHUB_TOKEN} \
            -Dtech-lab-ingester.libraries.io.token=${LIBRARIES_IO_TOKEN} \
            -Dtech-lab-ingester.stackexchange.api.key=${STACKEXCHANGE_TOKEN} \
            -Dtech-lab-ingester.bigQuery.projectId=${GCP_PROJECT_ID}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          LIBRARIES_IO_TOKEN: ${{ secrets.LIBRARIES_IO_TOKEN }}
          STACKEXCHANGE_TOKEN: ${secrets.STACKEXCHANGE_TOKEN}
          GCP_PROJECT_ID: ${secrets.GCP_PROJECT_ID}
